# ── ZEALWISH 活世界 · 轻后端配置(复制为 .env 后填写)──
# 这些密钥只留在后端,前端永远拿不到。

# 监听端口 + 允许跨域来源(前端 dev 地址)
PORT=8788
ALLOW_ORIGIN=http://localhost:5276

# ── B · 真 Claude 大脑 ──
# 在 https://console.anthropic.com 申请。留空则 /api/cognition/* 返回 503,前端自动回退到离线脚本引擎。
ANTHROPIC_API_KEY=
# 群演(逐 tick 决策)/ 导演(反思)模型
CROWD_MODEL=claude-haiku-4-5
DIRECTOR_MODEL=claude-opus-4-8

# ── C · 真 Base Sepolia 链 ──
# Base Sepolia 测试网 RPC(公共节点即可)。
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# 一个有 Base Sepolia 测试币的钱包私钥(0x 开头)。用水龙头领测试币:https://www.alchemy.com/faucets/base-sepolia
# 留空则 /api/chain/* 返回 503,前端自动回退到 mock 记账。
DEPLOYER_PRIVATE_KEY=
# 可选:已部署的 ERC-721 合约地址(带 mint(address,string) 方法)。留空则铸造走"calldata 记录"降级方案。
NFT_CONTRACT_ADDRESS=
